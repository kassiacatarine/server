language: python
python: 
  - "3.5"
cache: pip
sudo: false

install:
  - pip install -r requirements.txt
  - pip install coveralls

before_script:
  - "python manage.py makemigrations"
  - "python manage.py migrate"

script:
  - coverage run --source=server, py.test

after_success:
  - coveralls