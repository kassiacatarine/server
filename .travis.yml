language: python
python: 
  - "3.5"
cache: pip
sudo: false

install:
  - pip install -r requirements.txt

before_script:
  - "python manage.py makemigrations"
  - "python manage.py migrate"

script:
  - flake8 cmdbox
  - setup.py test --settings=cmdbox.tests_settings